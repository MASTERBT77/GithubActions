name: Issue Workflow 2
on:
  issues:
    types: [opened, edited]
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Output Github contex
        run: echo "${{ toJSON(github.event) }}"

